# Client application for controlling feature flags

# Pre requisites

Supported node version: 22.13.1
Install pnpm globally:
```
  npm i -g pnpm
```

# Running the project

Run the project by executing the command:

```
  pnpm dev
```

Open the browser at:

```
localhost:5173
```
